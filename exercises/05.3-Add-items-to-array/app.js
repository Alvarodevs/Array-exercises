var arr = [4,5,734,43,45];

// Your code here, use the push function and the random function.
    var randomNumber = arr.push (Math.floor(Math.random() * (9 - 0) + 0));
    var randomNumber2 = arr.push (Math.floor(Math.random() * (9 - 0) + 0));
    
console.log(arr);